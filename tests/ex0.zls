let node main() returns (o)
  local m default 0, half, last r
  do
    if half then do done else do m = 42 -> pre m + 1 done
  and
      o = m
  and
      half = true -> not (pre half)
  and
      r = m
  done
      
