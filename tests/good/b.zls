let node p(x, y) returns (o)
  o = (0.0 fby o) +. x *. y

let node sum_(s)() returns (o)
  forward(s)[i] (xi in [|1;2;3;4|]) returns (o init 0) do
    o = last o + i
  done

let node main() = run (sum_(4)) ()
