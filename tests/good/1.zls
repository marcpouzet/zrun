let x = [|[|1;2;3|];
          [|4;5;6|];
	  [|7;8;9|]|]
	  
let sym_x =
        [|[|1;4;7|];
	  [|4;5;8|];
	  [|7;8;9|]|]

let ff<<n>>() returns (low)
  forward<<n>>[i]() returns (lowi out low)
    do forward(i)[j]() returns (l1 out lowi)
          do l1 = j done
    done

let main2() =
  let y = ff<<3>>(x) in
  x, y
