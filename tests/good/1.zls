let main2() returns (low)
  forward <<0>>[i]() returns (low_i out low)
    do low_i = i done

(* 0; 1; 2
[| low0; low1; low2 |]

[||];
[|0|]
[|0;1|] *)

(*
let x = [|[|1;2;3|];
          [|4;5;6|];
	  [|7;8;9|]|]
	  
let sym_x =
        [|[|1;4;7|];
	  [|4;5;8|];
	  [|7;8;9|]|]

let ff<<n>>() returns (low)
  forward<<n>>[i]() returns (lowi out low)
    do foreach(i)[j]() returns (l1 out lowi)
          do l1 = j done
    done

let fff<<n>>() returns (low)
  forward<<n>>[i]() returns (lowi out low)
    do lowi = i done

*)