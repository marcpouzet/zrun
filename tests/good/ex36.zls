let f x y = x + y

let ff x = (x, x)

let node main4 () =
  let static x = ff 42 in
  (1, 2)

let node main4 () =
  let static x = ff 42 in
  let static y = 42 in
  inline (f y) (y + 1), x

let node main4 () =
  let static x = (1, 2) in
  x, x

